# Obsidian plugin settings — contains absolute paths, cookie source and playlist URLs
data.json

# macOS folder metadata
.DS_Store

# Node dependencies
node_modules/

# Local Claude Code config and personal working notes
.claude/

# Release build output; produced by `npm run build`, attached to releases
dist/
